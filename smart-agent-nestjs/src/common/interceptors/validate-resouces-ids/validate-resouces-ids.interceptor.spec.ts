import { ValidateResoucesIdsInterceptor } from './validate-resouces-ids.interceptor';

describe('ValidateResoucesIdsInterceptor', () => {
  it('should be defined', () => {
    expect(new ValidateResoucesIdsInterceptor()).toBeDefined();
  });
});
